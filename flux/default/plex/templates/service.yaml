apiVersion: v1
kind: Service
metadata:
  name: plex
spec:
  type: {{ .Values.service.type }}
  selector:
    app: plex
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 32400
