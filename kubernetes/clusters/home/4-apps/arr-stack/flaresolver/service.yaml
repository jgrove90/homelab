apiVersion: v1
kind: Service
metadata:
  name: flaresolver
  namespace: arr-stack

spec:
  selector:
    app: flaresolver

  ports:
    - name: http
      protocol: TCP
      port: 8191
      targetPort: 8191

  type: ClusterIP