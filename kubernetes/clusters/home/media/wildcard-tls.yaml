apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-alarlab
  namespace: media

spec:
  secretName: wildcard-alarlab-tls

  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer

  dnsNames:
    - "*.alarlab.dev"
    - "alarlab.dev"
    - "packetsandpods.com"