apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prowlarr-pvc
  namespace: arr-stack

spec:
  accessModes:
    - ReadWriteMany

  resources:
    requests:
      storage: 10Gi

  volumeName: prowlarr-pv

  storageClassName: ""