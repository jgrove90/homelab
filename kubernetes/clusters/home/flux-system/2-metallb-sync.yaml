apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization

metadata:
  name: metallb-system
  namespace: flux-system

spec:
  interval: 1h

  path: ./kubernetes/clusters/home/2-infra/metallb-system

  prune: true
  wait: true

  dependsOn:
    - name: repositories

  sourceRef:
    kind: GitRepository
    name: flux-system