apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: home
  namespace: flux-system
spec:
  interval: 5m
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  path: ./kubernetes/clusters/home
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
